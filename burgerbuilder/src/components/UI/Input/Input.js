import React from 'react';

const input =(props)=>{
    switch(props.inputType){
        
    }
    return(
        <div>
            <label>{props.label}</label>
        </div>
    );
}

export default input;